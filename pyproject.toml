[project]
name = "atlas"
version = "0.1.0"
description = "Atlas — executive intelligence platform, multi-agent prototype"
requires-python = ">=3.12,<3.15"
dependencies = [
    "anthropic>=0.103.1",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
